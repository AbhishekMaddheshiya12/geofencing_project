services:
  backend:
    build:
      context: ./backend
    environment:
      HTTP_ADDR: ":8080"
      DATABASE_URL: "postgresql://neondb_owner:npg_DPHJfdF1Kk4Z@ep-spring-mountain-atcoevr4.c-9.us-east-1.aws.neon.tech/neondb?sslmode=require"
      CORS_ALLOWED_ORIGIN: "*"
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE: "http://localhost:8080"
        VITE_WS_URL: "ws://localhost:8080/ws/alerts"
    ports:
      - "5173:80"
    depends_on:
      - backend
